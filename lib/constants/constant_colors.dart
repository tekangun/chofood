import 'package:flutter/material.dart';

class ConstantColors {
  var mainColor = Colors.orange;
  var whiteColor = Colors.white;
  var greenColor = Colors.green;
  var blackColor = Colors.black;
  var softWhiteColor = Colors.white.withOpacity(.8);
  var selectedGreen = Colors.green.withOpacity(.8);
  var skipBoxColor = Colors.blue.withOpacity(.4);
  var greyBoxColor = Colors.grey.withOpacity(.2);
}
